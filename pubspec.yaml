name: kartu_profil_mahasiswa
description: "Latihan Individu - Kartu Profil Mahasiswa (Flutter)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/foto_profil.jpg
    - assets/images/foto_saya.jpg
